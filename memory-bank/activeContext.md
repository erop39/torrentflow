# Active context

Current focus: release handoff after final static QA. Smart rules support TorrentLeech metadata conditions and qBittorrent routing; per-feed proxy, disk alerting, and versioned configuration export/import are implemented. Documentation and TODO now reflect the current behaviour and review findings.

Recent implementation: RSS scheduler, persistent category/seed fields, `notify`/`auto_add`/`both` actions, qBittorrent and Telegram adapters, audit events, configurable release-category catalog/default filter, Smart Auto-Add fields, TorrentLeech/proxy adapter support, disk monitoring, configuration backup APIs, and documentation reconciliation after review. Alembic runs through `20260826_08`.

Known gaps: proxy URLs currently allow embedded credentials, which would be persisted and exported; until this is fixed, use an unauthenticated local proxy endpoint. Tracker passkey/cookie storage is intentionally not implemented because no encrypted persistence design exists. Adapter integration tests need real local HTTP/SOCKS fixtures. The accidentally exposed Telegram token was removed from Git history and the rewritten history was force-pushed, but the token must still be revoked and regenerated in BotFather before any live Telegram test. Docker Desktop must be installed and running before the local Compose test; `scripts/setup-docker-env.sh` is ready to create the untracked `.env` with hidden input.
