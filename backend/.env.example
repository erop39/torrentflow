# Required for the local administrator session
TORRENTFLOW_ADMIN_PASSWORD=change-this-password
TORRENTFLOW_SESSION_SECRET=replace-with-a-long-random-secret

# qBittorrent Web API (optional until auto-add is enabled)
TORRENTFLOW_QBITTORRENT_URL=http://127.0.0.1:8080
TORRENTFLOW_QBITTORRENT_USERNAME=admin
TORRENTFLOW_QBITTORRENT_PASSWORD=replace-me

# Telegram (optional until notify/both rules are enabled)
TORRENTFLOW_TELEGRAM_BOT_TOKEN=
TORRENTFLOW_TELEGRAM_CHAT_ID=

# Required only when storing tracker cookies/passkeys through the API.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TORRENTFLOW_ENCRYPTION_KEY=

# Disk monitoring (percent free on TORRENTFLOW_DISK_PATH, or the app data path)
TORRENTFLOW_DISK_FREE_THRESHOLD_PERCENT=10
